#!/bin/bash

includes="$1"
destination="$2"

while IFS= read -r line; do
    echo "$line"
    rsync -av --delete --relative -b --backup-dir="$destination/trash" "$line" "$destination"
done < "$includes"
