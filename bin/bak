#!/bin/bash

if [ "$1" = '--move' ]; then
    shift
    while [ $# != 0 ]; do
        mv -i "$1" "$1.bak.$(date +%s)"
        shift
    done
else
    while [ $# != 0 ]; do
        cp -ir "$1" "$1.bak.$(date +%s)"
        shift
    done
fi
